#!/bin/sh

#############################
##      SHELL ALIASES      ##
#############################

# shortcuts
alias a='aliases'
alias b='$BROWSER'
alias c='cointop'
alias e='envs'
alias f='fn'
alias g='git'
alias m='neomutt'
alias o='open'
alias p='profile'
alias q='exit'
alias v='vim'
alias y='yay'

# files
alias rename='perl-rename'
alias open='xdg-open'
alias tree='tree --charset=ascii'

# history
alias hist='cat $HISTFILE'
alias hgrep='cat $HISTFILE | grep'

# ls
alias l='ls'
alias la='LC_ALL=C ls -lh --human-readable --group-directories-first --almost-all'
alias lh='LC_ALL=C ls -lh --human-readable --group-directories-first'
alias l1='LC_ALL=C ls -1'

# exa
alias exa='exa --color=never'
alias la='exa -lhgH --group-directories-first --all'
alias ll='exa -lhgH --group-directories-first'

# SHELL
alias bash='SHELL=/bin/bash bash'
alias zsh='SHELL=/bin/zsh zsh'

# git
alias ga='git add'
alias gc='git commit'
alias gd='git diff'
alias gf='git fuzzy'
alias gs='git status --short'
alias gl='git log --oneline'
alias gg='git log --oneline --graph --full-history --all'

# pacman
alias pqeq='pacman -Qeq'
alias pss='pacman -Ss'
alias ppr='doas pacman -Rsn'
alias pp='doas pacman -Syu'

# yay
alias yqeq='yay -Qeq'
alias yss='yay -Ss'
alias yyr='yay -Rsn'
alias yy='yay -Syu'

# docker
alias dk='docker'
alias dke='docker exec'
alias dkei='docker exec -it'
alias dker='docker exec --user root'
alias dkeir='docker exec -it --user root'
alias dkps="docker ps --format '{{.Names}} {{.Image}}'"

# docker compose
alias dup='docker-compose up -d'
alias ddown='docker-compose down'

# npm
alias naudit='npm audit'
alias nauditf='npm audit fix'
alias npublish='npm publish --access=public'

# npm scripts
alias nbuild='npm run build'
alias ndev='npm run dev'
alias ndev-js='npm run dev-js'
alias ndev-css='npm run dev-css'
alias nprod='npm run prod'
alias nprod-js='npm run prod-js'
alias nprod-css='npm run prod-css'
alias nserve='npm run serve'
alias nwatch='npm run watch'
alias nwatch-js='npm run watch-js'
alias nwatch-css='npm run watch-css'

# LARAVEL
alias pa='php artisan'
alias pam='php artisan migrate'
alias par='php artisan route:list --compact'
alias tinker='php artisan tinker'

# XDG
alias ssh='ssh -F ${XDG_CONFIG_HOME}/ssh/config'
alias scp='scp -F ${XDG_CONFIG_HOME}/ssh/config'
alias mbsync='mbsync -c ${XDG_CONFIG_HOME}/mbsync/mbsyncrc'
alias startx='startx "${XDG_CONFIG_HOME}/X11/xinitrc" -- "${XDG_CONFIG_HOME}/X11/xserverrc" vt1'

# clipboard
alias xcopy='xclip -selection clipboard'
alias xpaste='xclip -out -selection clipboard'

# more shortcuts
alias xx='(pidof dwm && pkill dwm; exit) || startx'
alias vv='vim ~/.config/vim'
alias dv='dvtm'
alias py='python'

# other stuff
alias notification-daemon='/usr/lib/notification-daemon-1.0/notification-daemon'
